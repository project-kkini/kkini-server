spring:
  profiles:
    active: ${profile}
    group:
      test: "test"
      local: "local"
      dev: "dev, datasource"
      prod: "prod, datasource"
    include:
      - security

logging:
  level:
    root: INFO
    org.springframework.security: TRACE
